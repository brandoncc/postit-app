<% breadcrumbs = link_to('All Posts', posts_path) + " &raquo; #{@category.name}".html_safe %>
<%= render 'shared/content_title', title: breadcrumbs %>

<%= render partial: 'shared/post', collection: @category.posts, as: :post %>
